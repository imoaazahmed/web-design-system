import { toRem } from '../../utils';

export const spaces = {
  '4xl': toRem(128),
  '3xl': toRem(96),
  '2xl': toRem(80),
  xl: toRem(48),
  'lg-alt': toRem(32),
  lg: toRem(24),
  md: toRem(16),
  sm: toRem(12),
  xs: toRem(8),
  '2xs': toRem(4),
};

export default spaces;
